# SO-ARM101 작업 안전 규칙

이 디렉터리는 SO-ARM101, LeRobot, 그리고 향후 ROS 2 연동만을 위한 전용 작업 공간이다.

## 원칙 적용

- 이 디렉터리의 문서는 기본 운용 원칙으로 준수한다.
- 문서 원칙 때문에 올바르거나 직접적인 해결책을 무작정 배제하지 않는다.
- 원칙과 해결책이 충돌하면 가정, 위험, 영향 범위, 복구 방법을 설명하고 사용자에게 재질문하여 명시적인 확인을 받은 후 진행한다.
- 확인된 예외가 반복되면 config/profile 또는 ADR로 정식 반영한다.
- 이 예외 절차는 기존 운영 서비스 중단, 데이터 손실, 회복 불가능한 하드웨어 변경을 암묵적으로 허가하지 않는다.

## 서버 Docker 보호

- 이 서버에는 SO-ARM101과 관계없는 운영 Docker 컨테이너가 이미 실행 중이다.
- SO-ARM101 작업 중 기존 컨테이너를 `stop`, `restart`, `rm`, `compose down` 또는 재배포하지 않는다.
- Docker가 필요해질 경우에도 이 디렉터리 안의 별도 Compose 프로젝트와 별도 컨테이너 이름만 사용한다.
- 기존 Docker 네트워크, 볼륨, 이미지, Compose 파일은 변경하지 않는다.
- 하드웨어(USB 모터 허브·카메라)는 SO-ARM101 전용 프로세스/컨테이너에만 명시적으로 연결한다.

## 현재 단계

- LeRobot을 이 디렉터리의 독립 Python 가상환경에 설치한다.
- ROS 2와 Docker 기반 LeRobot 실행은 하드웨어 단독 동작 확인 후 별도로 구성한다.
