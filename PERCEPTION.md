# 카메라 두 대로 큐브의 3D 좌표

시뮬에서 배운 정책은 물체 위치를 물리 엔진에서 공짜로 받는다
(`object_position_in_robot_root_frame`). 실물 팔에는 그것을 말해 줄 것이 없어서, 고정
카메라 두 대에서 **`base_link` 기준 (x, y, z) 세 숫자**를 만들어 낸다. 자세는 만들지
않는다 — 정책의 관측에 없고, 턱 개구 43mm가 큐브 대각선 31mm보다 커서 yaw가 어긋나도
물린다.

## 지금 상태 (2026-09-06)

| | 상태 |
|---|---|
| 기하 코드와 단위 시험 | **끝났다.** 합성 데이터로 검증(`tests/test_perception_*.py`, 17개) |
| API·모드·앱 화면 | **끝났다.** `/api/perception`, 캘리브레이션 두 단계, 맥 앱 `카메라 위치 추정` 화면 |
| 실물 내부 파라미터 | **아직.** 보드를 인쇄해 모아야 한다 |
| 실물 외부 파라미터 | **아직.** 그리퍼에 마커를 붙이고 팔을 돌려야 한다 (사람이 옆에 있을 때) |
| 책상 높이 `table_z` | **아직 재지 않았다.** `null`이고, 그래서 한 대만 보일 때 좌표를 내지 않는다 |
| 작업 영역 ROI | **비어 있다.** 측면 카메라는 ROI 없이는 팔의 주황 부품을 큐브로 볼 수 있다 |

## 구조

추정기는 **모드가 아니다.** 장치 하나를 두 프로세스가 열 수 없으므로(`owner_lock.py`),
카메라를 여는 상주 perception은 수집·정책과 공존할 수 없다. 그래서 추정기는 장치를 모르는
순수 객체이고, 그때그때 **프레임을 쥔 쪽**이 `PerceptionHub.offer()`로 넣는다.

| 상황 | 프레임의 주인 | 넣는 곳 |
|---|---|---|
| 아무 모드도 안 돎 | 콘솔의 `CameraWorker` | `cameras.py`의 캡처 루프 (`observer`) |
| 캘리브레이션이 돎 | `perception/calibrating.py` 자식 | 자기가 연 `VideoCapture` |
| 수집·정책이 돎 | 그 자식 | (아직 배선하지 않았다 — `_RateTrackedRobot` 자리) |

**따라서 프리뷰가 꺼져 있으면 좌표도 멈춘다.** 그때 `/api/perception`은 값을 내지 않고
`카메라 프리뷰를 켜면 좌표를 계산합니다`라고 적는다. 프리뷰와 무관하게 카메라를 계속 여는
쪽이 편하지만, 그러면 장치 lock을 영구히 쥐어 수집과 정책이 시작되지 못한다.

캘리브레이션은 반대로 두 성질이다.

- **내부 파라미터(렌즈)** — 모드가 아니다. 사람이 보드를 흔들 뿐 팔은 가만히 있고,
  MJPEG 프리뷰가 살아 있어야 사람이 무엇을 찍는지 본다.
- **외부 파라미터(자리)** — 다섯 번째 모드다. 팔이 혼자 20~25 자세로 움직이므로 owner
  lock · 409 충돌 검사 · `모드 중지`를 다른 넷과 똑같이 지나고, 확인 문구는
  `CALIBRATE SOARM101`이다.

## 파일

```
src/soarm_console/perception/
  kinematics.py    URDF 정기구학. 뷰어와 같은 so101.urdf를 읽는다. placo 의존성 없음
  board.py         ChArUco 보드와 그리퍼 마커의 치수 (맥 앱의 인쇄 스크립트와 같은 값)
  intrinsics.py    장 모으기 · rational/fisheye 둘 다 풀어 좋은 쪽 채택 · 픽셀→광선
  extrinsics.py    손-눈(robot-world) 초기해 + 재투영 최소화 정제
  poses.py         캘리브레이션이 설 자세 생성 (관절 한계 · 작업 영역 · 회전 다양성)
  cube.py          HSV 검출 · 광선 · 삼각측량 · 평면 구속 · 가시면 편향 보정
  estimator.py     ObjectEstimator — 장치를 모른다
  hub.py           프레임 받는 자리 + 일꾼 스레드 + 내부 파라미터 수집
  store.py         runtime/perception/rig.json
  calibrating.py   외부 파라미터 모드의 자식 프로세스
src/soarm_console/perception_manager.py   그 서브프로세스의 소유자
```

## 실측한 것

### 카메라 (2026-09-06, 실제 프레임)

| 항목 | 값 |
|---|---|
| 장치 | Waveshare IMX335 5MP 두 대, **2592×1944까지** 지원. 지금은 640×480 |
| 왜곡 | 두 대 모두 배럴 왜곡 뚜렷 (테이블 직선 모서리가 눈에 띄게 휜다) |
| `scene` | 테이블 위 **비스듬한 부감**. 22mm 큐브가 22×20 px → 약 **1 mm/px** |
| `wrist` | 이름과 달리 **책상 높이의 고정 측면 카메라**. 큐브 16×12 px |
| 색 검출 | 기본 HSV 문턱으로 `scene`에서 큐브가 유일하게 잡힌다(실측 확인). `wrist`에는 팔 베이스의 주황 부품이 함께 잡히므로 ROI 필요 |

### 솔버 (합성 데이터, 정답을 아는 자리)

손-눈 캘리브레이션 — 자세 24개, 카메라 중심 오차:

| 코너 검출 잡음 | 카메라 중심 오차 | 재투영 잔차 |
|---|---|---|
| 0.00 px | 0.00 mm | 0.000 px |
| 0.25 px | 0.36 mm | 0.335 px |
| 0.50 px | 0.73 mm | 0.670 px |
| 1.00 px | 1.45 mm | 1.341 px |

가시면 중심 편향 — **처음 문서에 "수 mm"라고 적었던 것은 틀렸다:**

| 경우 | 보정 전 | 보정 후 |
|---|---|---|
| 부감 0.60m · 평면 구속 | 0.10 mm | 0.00 mm |
| 부감 0.40m · 평면 구속 | 0.16 mm | 0.00 mm |
| 45° 부감 · 평면 구속 | 0.20 mm | 0.00 mm |
| 부감+측면 삼각측량 | 0.38 mm | 0.00 mm |

큐브가 22mm로 작고 카메라가 0.4~0.6m 떨어져 있어 원근이 실루엣에 주는 영향이 그만큼
작다. 이 보정은 오차 예산 5mm의 10% 이하를 다루는 마무리이지 승부처가 아니다. 승부처는
내부·외부 파라미터다.

### URDF

- 정기구학 사슬은 다섯 관절이다: `shoulder_pan · shoulder_lift · elbow_flex · wrist_flex ·
  wrist_roll`. `gripper`는 움직턱으로 갈라지는 가지라 손끝까지의 사슬에 없다.
- 관절이 모두 0이면 손끝이 `base_link` 기준 (391, 0, 227) mm다.
- 어깨 회전의 중심은 베이스 원점이 아니라 **x = 38.8mm**에 있다.
- `rpy`에 π가 `3.14159`로 잘려 있어 어깨 회전축이 수직에서 약 1e-6 rad 기울어 있다.
  60° 돌리면 손끝이 0.5µm 오르내린다 — 실재하지만 이 작업에서는 뜻이 없다.

## 아직 재지 않은 것, 그리고 열려 있는 물음

### 1. 시뮬과 실물의 `shoulder_pan` 부호가 어긋나 보인다 ⚠️

**아직 확인하지 못했고, 확인하기 전에는 시뮬 좌표와 실물 좌표를 직접 견주면 안 된다.**

- 이 URDF의 정기구학으로는 `shoulder_pan = -0.555 rad`일 때 손끝이 `base_link` 기준
  **+Y**(338.5, +185.8, …) mm로 간다.
- 시뮬(`joint_pos_env_cfg.py`)은 같은 `HOME_PAN = -0.555`가 집는 자리
  `PICK_XY = (0.164, 0.108)` 월드 = 루트 기준 (0.108, **−0.164**)를 향하도록 골랐다고
  적어 두었다.

즉 같은 관절값이 루트 프레임에서 반대쪽 Y를 가리킨다. 이유로 짐작되는 것은 **에셋이 서로
다르다**는 사실이다 — 시뮬은 NVIDIA가 배포하는 `Robots/RobotStudio/so101_new_calib.usd`를
쓰고, 콘솔은 `renesas-rdk/so_arm101_description`의 URDF를 쓴다. URDF의 `shoulder_pan`
원점은 `rpy="3.14159 0 -3.14159"`로 국소 Z축이 아래를 향하는데, USD 변환이 이 뒤집힘을
정규화했다면 부호가 갈린다.

이것이 중요한 이유는 perception이 아니라 **정책 이식** 때문이다. 이 저장소의 파이프라인은
실물 팔의 좌표계 안에서 자기 완결적이다(카메라를 팔의 정기구학에 맞춰 잡으므로). 그러나
시뮬에서 배운 정책을 실물에 올릴 때는 관절 부호가 같아야 한다. 확인하려면 시뮬을 한 스텝
돌려 `shoulder_pan = -0.555`에서 손끝의 루트 좌표를 찍어 보면 된다(GPU 큐를 통해서).

그때까지 `PERCEPTION.md`와 작업지시의 "시뮬 대조값 (0.108, −0.164, 0.005)"은 **검증된
목표가 아니라 확인해야 할 물음**이다.

### 2. 탑뷰가 작업 영역을 다 담는가

1 mm/px에 도달 가능 영역이 지름 556mm면 480px 프레임보다 크다. 지금 프레임에서 큐브와
노란 목표는 둘 다 보이지만, 큐브가 놓일 수 있는 **전 범위**가 두 카메라에 들어오는지
캘리브레이션 전에 확인해야 한다. **캘리브레이션 뒤에 카메라를 건드리면 전부 무효다.**

### 3. 노출 값

`PERCEPTION_CONTROLS`의 `exposure_time_absolute = 167`은 조명 조사에서 잰 값이다
(120Hz 깜빡임 주기 8.33ms의 정확히 2배). **그 방의 그때 빛에서 다시 재고** 83·167·250·333
중 포화 없이 가장 밝은 것을 고른 뒤 이 값을 고친다. `RECORDING_CONTROLS`는 건드리지 않는다 —
수집에 노출 고정을 넣을지는 사용자가 보류한 결정이다.

### 4. 수집·정책 중의 추정

지금은 콘솔의 `CameraWorker`만 프레임을 넣는다. 수집·정책이 도는 동안에도 좌표를 보려면
`recording.py`의 `_RateTrackedRobot`에서 같은 `offer()`를 부르면 된다. 그때 관측은 **RGB**
이므로 BGR로 바꿔서 넘겨야 한다.

## 절차

### 0. 준비 (사람)

1. 카메라 두 대를 흔들리지 않게 고정한다.
2. 맥 앱 저장소에서 인쇄물을 만들고 100% 배율로 인쇄한다.
   `.venv-docparse/bin/python scripts/make-calibration-targets.py ~/Desktop`
3. 함께 찍힌 100mm 확인선과 사각형 한 변을 캘리퍼로 재서 실제 값을 `rig.json`에 넣는다.
4. 그리퍼에 40mm 마커를 **단단히** 붙인다. 흰 여백을 잘라내지 않는다.

### 1. 렌즈 (팔은 움직이지 않는다)

맥 앱 `카메라 위치 추정` 화면 → 카메라를 고르고 `보드 모으기 시작` → 화면의 3×3 격자가
다 찰 때까지 보드를 **구석까지** 움직인다 → `풀기`.

### 2. 자리 (팔이 혼자 움직인다 — 사람이 옆에 있을 때만)

두 카메라의 렌즈가 끝나면 `캘리브레이션 시작` → 체크박스 확인 → 팔이 자세를 돌며 찍는다.
잔차가 화면에 px와 mm로 뜬다.

### 3. 책상 높이

두 카메라의 자리가 잡히면 큐브를 책상 위 여러 자리에 두고 삼각측량한 z의 중앙값에서
큐브 반높이(8mm)를 빼 `table_z`에 넣는다.

### 4. 검증 — 이 작업의 정직한 오차 숫자

큐브를 대여섯 자리에 두고 `/api/perception`의 값을 적는다. 같은 자리에서 그리퍼 끝을 큐브
중심 바로 위에 맞춘 뒤(텔레옵으로 사람이) 그때의 정기구학 좌표를 적는다. 두 좌표의 차이가
이 리그의 실제 오차다. **XY 5mm 이내가 목표**이고, 넘으면 내부 RMS·외부 잔차·편향 보정 중
어디서 깨졌는지를 나눠 적는다. 결과를 이 문서의 표로 남긴다.
